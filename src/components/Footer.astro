---
import type { SocialLink } from '@types/index';

const currentYear = new Date().getFullYear();

const socialLinks: SocialLink[] = [
  { platform: 'facebook', url: 'https://facebook.com' },
  { platform: 'instagram', url: 'https://instagram.com' },
  { platform: 'twitter', url: 'https://twitter.com' },
  { platform: 'linkedin', url: 'https://linkedin.com' },
];

const footerLinks = [
  {
    title: 'Compañía',
    links: [
      { label: 'Acerca de', href: '/about' },
      { label: 'Blog', href: '/blog' },
      { label: 'Contacto', href: '/contact' },
      { label: 'Carrera', href: '#' },
    ],
  },
  {
    title: 'Eventos',
    links: [
      { label: 'Próximos', href: '/events' },
      { label: 'Galería', href: '/gallery' },
      { label: 'Testimonios', href: '#testimonials' },
      { label: 'FAQ', href: '#faq' },
    ],
  },
  {
    title: 'Legal',
    links: [
      { label: 'Privacidad', href: '#' },
      { label: 'Términos', href: '#' },
      { label: 'Cookies', href: '#' },
      { label: 'Sitemap', href: '/sitemap.xml' },
    ],
  },
];
---

<footer class="bg-eventflow-black py-16 text-white md:py-24 relative overflow-hidden">
  <div class="footer__shape overflow-hidden"></div>
  <div class="container-custom relative z-10">
    <!-- Footer Content -->
    <div class="mb-12 grid grid-cols-1 gap-8 md:grid-cols-2 lg:grid-cols-4">
      <!-- Company Info -->
      <div>
        <div class="mb-6 flex items-center space-x-2 group">
          <div class="h-10 w-10 rounded-lg bg-gradient-to-br from-eventflow-primary to-eventflow-base flex items-center justify-center font-bold transition-transform duration-300 group-hover:scale-110">
            E
          </div>
          <span class="text-xl font-bold transition-colors duration-300 group-hover:text-eventflow-primary">EventFlow</span>
        </div>
        <p class="mb-6 text-sm text-white/80">
          Plataforma profesional de gestión de eventos y conferencias para conectar, comprometer e inspirar.
        </p>
        <!-- Social Links -->
        <div class="flex space-x-4">
          {
            socialLinks.map((social) => (
              <a
                href={social.url}
                class="flex h-10 w-10 items-center justify-center rounded-full bg-white/10 transition-colors hover:bg-eventflow-primary"
                aria-label={social.platform}
                target="_blank"
                rel="noopener noreferrer"
              >
                <span class="text-sm font-bold">
                  {social.platform === 'facebook' && '📘'}
                  {social.platform === 'instagram' && '📷'}
                  {social.platform === 'twitter' && '𝕏'}
                  {social.platform === 'linkedin' && '💼'}
                </span>
              </a>
            ))
          }
        </div>
      </div>

      <!-- Footer Columns -->
      {
        footerLinks.map((column) => (
          <div>
            <h3 class="mb-6 font-semibold">{column.title}</h3>
            <ul class="space-y-3">
              {column.links.map((link) => (
                <li>
                  <a
                    href={link.href}
                    class="text-sm text-white/80 transition-colors hover:text-eventflow-primary"
                  >
                    {link.label}
                  </a>
                </li>
              ))}
            </ul>
          </div>
        ))
      }
    </div>

    <!-- Divider -->
    <div class="my-8 border-t border-white/10" />

    <!-- Bottom Footer -->
    <div class="flex flex-col items-center justify-between space-y-4 text-sm text-white/60 md:flex-row">
      <p>
        &copy; {currentYear} EventFlow. Todos los derechos reservados.
      </p>
      <p>
        Hecho con ❤️ usando Astro + React + Tailwind CSS
      </p>
    </div>
  </div>
</footer>

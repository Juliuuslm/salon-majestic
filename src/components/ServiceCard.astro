---
interface Props {
  icon: string;
  title: string;
  description: string;
  features?: string[];
  link?: string;
}

const {
  icon,
  title,
  description,
  features = [],
  link = '#',
} = Astro.props;
---

<div class="service-card card-base border-glow wow fade-in-up" data-wow-duration="0.8s" data-wow-delay="0s">
  <div class="p-8">
    <div class="mb-4 flex h-16 w-16 items-center justify-center rounded-lg bg-eventflow-base/20 transition-all duration-500 hover:bg-eventflow-base/40 hover:scale-110">
      <span class="text-3xl transition-transform duration-500">{icon}</span>
    </div>

    <h3 class="mb-3 text-xl font-bold text-white">
      {title}
    </h3>

    <p class="mb-6 text-sm text-white/90 leading-relaxed">
      {description}
    </p>

    {features.length > 0 && (
      <ul class="mb-6 space-y-2">
        {features.map((feature) => (
          <li class="flex items-center gap-2 text-sm text-gray-300">
            <svg
              class="h-4 w-4 text-eventflow-primary"
              fill="currentColor"
              viewBox="0 0 20 20"
            >
              <path
                fill-rule="evenodd"
                d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z"
                clip-rule="evenodd"
              />
            </svg>
            <span>{feature}</span>
          </li>
        ))}
      </ul>
    )}

    <a
      href={link}
      class="inline-flex items-center gap-2 text-sm font-semibold text-eventflow-primary transition-colors hover:text-eventflow-base"
    >
      Más información
      <svg
        class="h-4 w-4"
        fill="none"
        stroke="currentColor"
        viewBox="0 0 24 24"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M9 5l7 7-7 7"
        />
      </svg>
    </a>
  </div>
</div>

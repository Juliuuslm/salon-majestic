---
import BaseLayout from '@layouts/BaseLayout.astro';
import HeroSlider from '@islands/HeroSlider';
import TestimonialsCarousel from '@islands/TestimonialsCarousel';
import ScrollAnimationWrapper from '@islands/ScrollAnimationWrapper';
import SectionTitle from '@components/SectionTitle.astro';
import ServiceCard from '@components/ServiceCard.astro';
import EventCard from '@components/EventCard.astro';
import CounterBox from '@islands/CounterBox';
import AwardsPress from '@components/AwardsPress.astro';
import services from '@data/services.json';
import events from '@data/events.json';
import testimonials from '@data/testimonials.json';

const heroSlides = [
  {
    id: '1',
    title: 'Tu Boda de Ensueño Comienza Aquí',
    subtitle: 'Elegancia, Lujo y Distinción',
    description:
      'Celebra el momento más importante en un salón de arquitectura privilegiada. Cada detalle diseñado para hacer tu boda inolvidable.',
    location: 'Majestic, Puebla',
    dateTime: 'Disponible todo el año - Consulta fechas',
    image: '/images/backgrounds/slider-1.jpg',
    ctaText: 'Agendar Cita',
    ctaLink: '/contact',
  },
  {
    id: '2',
    title: 'XV Años Espectaculares',
    subtitle: 'Crea Recuerdos Eternos',
    description:
      'Tu fiesta de XV años merece un escenario de lujo. Espacios versátiles, decoración mágica y ambientación de ensueño te esperan.',
    location: 'Majestic, Puebla',
    dateTime: 'Disponible todo el año - Consulta disponibilidad',
    image: '/images/backgrounds/slider-2.jpg',
    ctaText: 'Cotización XV Años',
    ctaLink: '/contact',
  },
  {
    id: '3',
    title: 'Eventos Corporativos de Impacto',
    subtitle: 'Infraestructura Premium para tu Empresa',
    description:
      'Conferencias, lanzamientos y actos empresariales con tecnología de vanguardia en la mejor venue de Puebla.',
    location: 'Majestic, Puebla',
    dateTime: 'Contacta para disponibilidad',
    image: '/images/backgrounds/slider-3.jpg',
    ctaText: 'Conocer Capacidades',
    ctaLink: '/services',
  },
];

const seo = {
  title: 'Majestic - El Salón de Eventos de Lujo en Puebla',
  description:
    'Bodas, XV años, eventos corporativos y banquetes gourmet en Majestic. Venue de lujo con arquitectura privilegiada y servicios integrales en Puebla.',
  image: '/og-home.jpg',
  schema: {
    '@context': 'https://schema.org',
    '@type': 'Organization',
    name: 'Majestic Puebla',
    url: 'https://majesticpuebla.com',
    logo: '/logo.png',
    description:
      'El salón de eventos de lujo en Puebla. Bodas, XV años, eventos corporativos y gastronomía de autor.',
  },
};
---

<BaseLayout seo={seo}>
  <!-- Hero Slider -->
  <div class="relative overflow-hidden">
    <div class="slider__shape slider__shape-1 overflow-hidden"></div>
    <div class="slider__shape slider__shape-2 overflow-hidden"></div>
    <div class="slider__shape slider__shape-3 overflow-hidden"></div>
    <HeroSlider slides={heroSlides} autoplay={true} delay={5000} client:load />
  </div>

  <!-- Services Section -->
  <section class="py-20 md:py-28 lg:py-32 bg-eventflow-black relative overflow-hidden">
    <!-- Floating shapes -->
    <div class="absolute top-20 right-10 opacity-20 pointer-events-none">
      <div class="w-32 h-32 bg-eventflow-primary/30 rounded-full blur-3xl float-bob-y"></div>
    </div>
    <div class="absolute bottom-20 left-10 opacity-20 pointer-events-none">
      <div class="w-40 h-40 bg-eventflow-base/20 rounded-full blur-3xl float-bob-x"></div>
    </div>

    <div class="container-custom relative z-10">
      <SectionTitle
        tagline="Nuestros Servicios"
        title="Servicios Integrales de Majestic"
        subtitle="Bodas, XV años, eventos corporativos y experiencias gourmet. Cada detalle cuidado con maestría."
        alignment="center"
      />

      <ScrollAnimationWrapper
        animationType="stagger"
        selector="[data-service-card]"
        staggerDelay={0.15}
        client:load
      >
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 mt-16">
          {
            services.slice(0, 6).map((service, index) => (
              <div data-service-card>
                <ServiceCard
                  icon={service.icon}
                  title={service.title}
                  description={service.description}
                  features={service.features}
                  link={service.link}
                  delay={`${(index + 1) * 100}ms`}
                />
              </div>
            ))
          }
        </div>
      </ScrollAnimationWrapper>
    </div>
  </section>

  <!-- Stats Section -->
  <section class="py-20 md:py-28 bg-eventflow-black text-white">
    <div class="container-custom">
      <SectionTitle
        tagline="Nuestro Impacto"
        title="Momentos Memorables Creados"
        alignment="center"
      />

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-12 mt-16">
        <CounterBox
          end={200}
          suffix="+"
          label="Eventos Celebrados"
          icon="🎉"
          client:visible
        />
        <CounterBox
          end={25000}
          suffix="+"
          label="Asistentes Felices"
          icon="💕"
          client:visible
        />
        <CounterBox
          end={100}
          suffix="%"
          label="Recomendaciones"
          icon="⭐"
          client:visible
        />
        <CounterBox
          end={20}
          suffix="+"
          label="Años de Excelencia"
          icon="👑"
          client:visible
        />
      </div>
    </div>
  </section>

  <!-- Upcoming Events -->
  <section class="py-20 md:py-28 bg-eventflow-black">
    <div class="container-custom">
      <SectionTitle
        tagline="Próximos Eventos"
        title="Celebraciones en Majestic"
        subtitle="Bodas, XV años y eventos corporativos que harán historia"
        alignment="center"
      />

      <ScrollAnimationWrapper
        animationType="stagger"
        selector="[data-event-card]"
        staggerDelay={0.15}
        client:load
      >
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 mt-16">
          {
            events.slice(0, 3).map((event, index) => (
              <div data-event-card>
                <EventCard
                  title={event.title}
                  date={event.date}
                  time={event.time}
                  location={event.location}
                  image={event.image}
                  description={event.description}
                  link={event.link || '/events'}
                  delay={`${(index + 1) * 100}ms`}
                />
              </div>
            ))
          }
        </div>
      </ScrollAnimationWrapper>

      <div class="mt-12 text-center">
        <a href="/events" class="btn-primary ripple-btn">
          Ver Todos los Eventos
        </a>
      </div>
    </div>
  </section>

  <!-- Testimonials Carousel -->
  <section class="py-20 md:py-28 bg-eventflow-black-dark relative overflow-hidden">
    <!-- Floating shapes -->
    <div class="absolute top-40 left-20 opacity-15 pointer-events-none">
      <div class="w-48 h-48 bg-eventflow-primary/30 rounded-full blur-3xl float-bob-x"></div>
    </div>
    <div class="absolute bottom-10 right-20 opacity-20 pointer-events-none">
      <div class="w-36 h-36 bg-eventflow-base/20 rounded-full blur-3xl float-bob-y"></div>
    </div>

    <div class="container-custom relative z-10">
      <SectionTitle
        tagline="Testimonios"
        title="Lo que Dicen Nuestros Clientes"
        subtitle="Experiencias reales de novios, quinceañeras y empresarios que confiaron en Majestic"
        alignment="center"
      />

      <div class="mt-16">
        <TestimonialsCarousel
          testimonials={testimonials}
          autoplayDelay={5000}
          client:load
        />
      </div>
    </div>
  </section>

  <!-- Awards & Press -->
  <AwardsPress
    title="Certificaciones y Reconocimientos"
    subtitle="Confianza de las mejores marcas y asociaciones profesionales en Puebla"
    logos={[
      { src: "🏆", alt: "Mejor Venue 2024" },
      { src: "⭐", alt: "5 Estrellas" },
      { src: "👑", alt: "Premium Certified" },
      { src: "🎖️", alt: "Excellence Award" },
      { src: "💎", alt: "Luxury Partner" },
      { src: "✨", alt: "Certified Premium" },
    ]}
  />

  <!-- CTA Section -->
  <section class="py-20 md:py-28 bg-gradient-to-r from-eventflow-primary to-eventflow-base text-white">
    <div class="container-custom text-center">
      <h2 class="text-4xl md:text-5xl font-bold mb-6">
        Haz tu Evento Majestic
      </h2>
      <p class="text-xl mb-10 max-w-2xl mx-auto">
        Contáctanos hoy y descubre cómo Majestic puede transformar tu visión en una celebración inolvidable.
      </p>
      <a href="/contact" class="ripple-btn inline-block bg-eventflow-black text-eventflow-primary px-8 py-4 rounded-full font-semibold hover:bg-eventflow-black-dark hover:shadow-lg transition-all duration-300">
        Agendar Consulta Gratis
      </a>
    </div>
  </section>
</BaseLayout>
